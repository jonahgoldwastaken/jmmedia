query services {
  services {
    slug
    listImage
    name
  }
}
query service($slug: String!) {
  service(slug: $slug) {
    name
    listImage
    description
    baseOptions
    additionalOptions
    callToAction
  }
}

query serviceSelect {
  services {
    _id
    name
  }
}
