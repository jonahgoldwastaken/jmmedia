{
  "builds": [
    { "src": "web/package.json", "use": "@now/next" },
    { "src": "server/package.json", "use": "@now/node-server" }
  ],
  "routes": [
    { "src": "/api", "dest": "server" },
    { "src": "/(.*)", "dest": "web" }
  ]
}
