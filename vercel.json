{
  "builds": [
    { "src": "web/package.json", "use": "@now/next" },
    { "src": "server/package.json", "use": "@now/node" }
  ],
  "routes": [
    { "src": "/api", "dest": "server" },
    { "src": "/(.*)", "dest": "web" }
  ]
}
